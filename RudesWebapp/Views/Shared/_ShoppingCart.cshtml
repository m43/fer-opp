<!-- Modal -->
<div class="modal fade" id="shoppingCartModal" tabindex="-1" role="dialog" aria-labelledby="shoppingCartModalTitle" aria-hidden="true">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="shoppingCartModalTitle">Košarica</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <button @@click="addArticleToCart(allArticles[3])">Dodaj artikl</button>
                <div>
                    Total price: {{ totalPrice }}kn
                </div>
                <p></p>
                <table v-if="cartContent.length != 0">
                    <tr>
                        <th></th>
                        <th>Name</th>
                        <th>Description</th>
                        <th>Type</th>
                        <th>Price</th>
                        <th>Quantity</th>
                        <th>Size</th>
                    </tr>

                    <tr v-for="item in cartContent">
                        <td>
                            <div class="deleteButton" @@click="removeArticleFromCart(item.article)">
                                <i class="fas fa-times"></i>
                            </div>
                        </td>
                        <td>{{ item.article.name }}</td>
                        <td>{{ item.article.description }}</td>
                        <td>{{ item.article.type }}</td>
                        <td>{{ item.article.price }}</td>
                        <td>{{ item.shoppingCartArticle.quantity }}</td>
                        <td>{{ item.shoppingCartArticle.size }}</td>
                    </tr>
                </table>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Zatvori</button>
                <button type="button" class="btn btn-primary">Spremi</button>
            </div>
        </div>
    </div>
</div>

<script src="~/js/shoppingCart.js"></script>